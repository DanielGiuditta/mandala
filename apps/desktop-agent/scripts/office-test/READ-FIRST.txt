MANDALA LAN TIME TRACKING 1.2.4 -- MAINTAINER ACCEPTANCE PACKAGE
Employee Agent 1.0.16; existing gateway 1.1.0 with the audited startup repair.

REQUIRES WINDOWS ADMINISTRATOR APPROVAL for installation or gateway repair.
Keep the entire extracted folder together. Open Start Mandala.cmd normally.

THIS OFFICE: STP54 is the gateway; STP32 is the employee computer.
The shared office clock is approximately 33 seconds behind production.
Do not start real time tests until its administrator corrects the source and
Daniel verifies the production-clock check. This package changes no clock.

FIRST -- DIAGNOSE WITHOUT INTERRUPTING WORK
1. On STP54 choose 5: gateway read-only checks. This does not repair or reboot.
2. On STP32, in the employee's own Windows account, choose 1: employee baseline.
   An older installed agent will correctly fail the version check.
3. Daniel reviews both reports. No employee or IT member needs to interpret
   logs, reinstall the gateway, change clocks or repeat a failed test.

EMPLOYEE UPDATE -- MAINTAINER OPERATED
Before updating, save other work and verify the Agent has no active work or
pending upload. Preserve existing reports, sign-in, pairing and time journals.
Run only MandalaAgentSetup-1.0.16.exe from this verified package, approve the
Windows administrator prompt, and retain the existing installation location.
Do not uninstall first. Return to the employee account and rerun baseline.
The installer requires administrator approval; the employee app/test must run
normally, not as administrator. Never clear data or replace pairing to proceed.

ONE CONTROLLED ACCEPTANCE SESSION -- ONLY AFTER PREREQUISITES PASS
Daniel operates this remotely when both PCs and access are available.
Choose 3 on STP32. Use the existing employee identity and two allowed projects.
The runner requires explicit confirmation before creating five real test
entries and before restarting Windows. It checks automatic startup, restored
sign-in, start/stop, project switch, five-minute idle pause and pending recovery.
Daniel verifies the exact saved references and durations in production.

Do not physically disconnect the employee LAN during a remote-only session:
that would also remove remote control. Stop at that scenario until a bounded,
automatically restored application-only interruption has been reviewed, or
someone is available to restore the cable. Never disconnect gateway internet.
The existing isolated tests already cover pending saves across app restart;
they are not evidence of this office's network/reboot behavior.

Do not repeat STP54's already-passed reboot merely because this kit is newer.
Option 2 is a repair/reboot path for a proven gateway failure, not a baseline.
Preserve completed/failed earlier runs. The runner will not replay their time
writes automatically. Daniel must review the previous state before a new run.

If anything fails, keep the report and stop that test. Option 4 exports existing
evidence without repairs or new time entries. Use the exact SEND THIS CURRENT
REPORT path shown. Preserve any native Agent diagnostic report too.

A running window, project list, successful export or isolated CI pass does not
clear an employee trial. Current clock, office tests, exact production saves,
and the published installer/download fingerprint must all be verified first.
