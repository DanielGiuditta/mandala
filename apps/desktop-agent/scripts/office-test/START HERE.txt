MANDALA COMPLETE TEST 1.2.0 - START HERE
ONE OFFICE SESSION. SEND BOTH REPORT ZIPS TOGETHER.

Allow roughly 25-35 minutes elapsed. Target: 5-10 minutes of employee attention
after setup; most timer checks run unattended. Setup problems may take longer.
Keep the employee PC unlocked and unused. Do not send screenshots at each step.

HAVE THESE READY
- IT/Windows administrator, the configured internet-connected gateway (1.1.0),
  and ONE LAN-only employee PC using its original Windows account.
- Employee's own login and two allowed projects agreed for FIVE real time entries.
- Existing employee pairing ZIP if connection setup is incomplete. It is specific
  to that Windows profile; this package cannot replace private keys.
- Internet-connected browser for the same employee's short ownership check.

1. EXTRACT THE WHOLE ZIP ON BOTH PCs
Keep the extracted folder in place through restarts. Do not run inside the ZIP.
Never add this test or the setup wizard to Startup.

2. GATEWAY FIRST
Open "Start gateway Test.cmd" and approve administrator access.
IT confirms the network checklist and that NO employee timers are active.
Save other work and approve the requested restart.
Sign back into the SAME administrator account. Approve the resumed test's prompt.
If the test does not reopen, open the same launcher. Do NOT open gateway setup or
start its task manually. Confirm whether it started by itself.
Continue only when the gateway report says local checks passed.

3. EMPLOYEE PC
Open "Start employee Test.cmd" NORMALLY (not as administrator).
Enter the email; sign in in the real Agent. Never type a password in the checker.
Approve installation/startup repair if requested. Choose two test projects by
number and authorize the five entries. Save other work and approve restart.
Sign back into the SAME employee account. The test reopens; if not, open its
launcher. Do NOT open Agent/setup manually. Confirm Agent opened by itself.
Sign in in Agent again only if requested.

4. RESPOND TO THREE SHORT PROMPTS; THE RUNNER DOES THE TIMER WORK
- Browser: same employee views project B and tries Start Work. Confirm viewing
  did not switch A AND the browser could not take over the desktop session.
- Disconnect ONLY the employee PC from LAN when asked. Confirm. The runner stops
  once, checks pending work, closes/reopens Agent and checks blocked new starts.
- Reconnect when called back. Confirm, then leave the PC unlocked and untouched.
  Switching and the real idle check run automatically for about 13 more minutes.
The runner generates small mouse movements during active test sessions, then
stops input for the six-minute idle check. Do not use this PC during the test.

5. SEND THESE TWO DESKTOP ZIPs TOGETHER ONCE
  Mandala-Quick-Test-<GATEWAY-PC>-gateway.zip
  Mandala-Quick-Test-<EMPLOYEE-PC>-employee.zip
Send both even if a check fails. They contain checks, IST/UTC actions and references.
Daniel verifies all FIVE actual database entries from these reports. Local checks
alone do not clear an office rollout. Keep any native Agent diagnostics file.

IF A CHECK FAILS
See FIXES.txt for known setup corrections. Before time tests begin, correct the
prerequisite and reopen the SAME launcher. After time tests begin, do not rerun,
reinstall or delete state. Send the reports; unattempted tests stay BLOCKED.
If disconnected when it stops, reconnect LAN and preserve active/pending work.
Hold Escape during automation to stop. Reopening an interrupted test exports its
evidence without repeating time writes. No passwords, keys or raw journals are
included in the reports; security checks/firewall settings are not changed.

INCLUDED APPROVED EMPLOYEE INSTALLER
MandalaAgentSetup-1.0.15.exe - Windows administrator approval required.
Production: nzlajptokbcgeaifgnoq.supabase.co
Existing unsigned pre-launch release: supervised test only. This is a test/repair
kit, not a new Agent or gateway release. Keep existing pairing material available.
