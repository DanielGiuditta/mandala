MANDALA - QUICK LAN CHECK 1.2.3

ONE EMPLOYEE. ONE PROJECT. ONE TWO-MINUTE TIME ENTRY.
Allow roughly 10-20 minutes, depending on Windows restart speed.

Use THIS instruction sheet for this visit. It replaces the longer READ-FIRST
procedure inside Mandala files. DO NOT choose option 3: that runs the long test.

Before starting: have Windows administrator approval, the original paired
employee Windows account, their Mandala login and one permitted project ready.
The gateway must already be configured. Save other work and ensure no employee
timers are active during gateway maintenance. Preserve any pending work.

1. EXTRACT THE WHOLE ZIP
Extract into a new folder on both PCs. Keep the Mandala files subfolder intact.
Whenever a step says open Start Mandala.cmd, find it INSIDE Mandala files.

2. EMPLOYEE PC - COLLECT INITIAL REPORT
In the original employee Windows account, open Start Mandala.cmd normally and
choose 1. Keep its report. NOT CLEARED is expected for this evidence-only mode.
It may show gateway failures before repair; keep the report and proceed to 3.

3. GATEWAY STP54 - REPAIR AND RESTART ONCE
Open Start Mandala.cmd and choose 2. Approve safe maintenance and the requested
restart. Sign back into the SAME administrator account. If it does not resume,
reopen the same launcher and choose 2. Do not open gateway setup or manually
start its task. Wait for the final GATEWAY LOCAL CHECKS PASSED result after reboot.
If this fails, STOP time testing and return the gateway and initial employee
reports. Do not repeat the repair or start an employee timer.

4. EMPLOYEE PC - SAVE ONE REAL ENTRY
Only after gateway success, open Mandala Agent normally in the original employee
account. It must be version 1.0.15 with backend nzlajptokbcgeaifgnoq.supabase.co.
Sign in. If the version is wrong, sign-in fails, or existing work is active or
pending, stop time testing and go to step 5. Do not overwrite existing work.
Choose ONE agreed project. This creates ONE real production entry. Click Start
Work, use the PC normally for TWO minutes, then click Stop once. Wait for
"Time saved successfully. Reference: ..." and "No active project".
A pending-upload message is not a saved result. If confirmation is absent after
two minutes or another error appears, stop time testing and go to step 5.
Do not start the timer again.

5. COLLECT THE RESULT AND RETURN TWO REPORTS
On the employee PC, open Start Mandala.cmd normally and choose 1 again. This
captures fresh evidence and the save reference; it does NOT repeat the timer.
Its NOT CLEARED heading remains expected because the full test was not run.
Send the CURRENT gateway report and LAST employee report shown by
SEND THIS CURRENT REPORT together. If Desktop saving fails, use the local path
shown instead. Include employee email, project, approximate start/stop time in
IST and whether the save confirmation appeared. No step-by-step screenshots.

Do not reinstall, re-pair, clear data or repeat an uncertain save. Preserve the
reports and current application state if anything stops.

Daniel will verify the exact new entry in production before confirming basic
LAN time saving. This short check does not accept employee automatic startup,
offline recovery, project switching, idle pausing or wider rollout.
